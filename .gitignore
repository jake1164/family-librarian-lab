.venv/
__pycache__/
*.py[cod]
lab.env
results/
repos/
# Generated by clients.ensure_sftp_test_keypair() -- a disposable, host-local
# SSH keypair for the cwa-sftp-key profile, not real deployment material.
runtime/
.ai_docs/

# External-provider plugin registry and per-provider Compose overlays --
# entirely untracked so a real/private integration's identity, repo URL, and
# secrets never appear in this public repo. See docs/ for the registry format.
external-providers.local.yaml
external-providers/

