server {
    listen 443 ssl;
    server_name gutenberg-fixture;

    ssl_certificate     /etc/nginx/ssl/server.pem;
    ssl_certificate_key /etc/nginx/ssl/server.key;

    root /usr/share/nginx/html;
    autoindex off;

    location / {
        try_files $uri =404;
    }
}
